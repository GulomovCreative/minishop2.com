//=require ../../../node_modules/jquery/dist/jquery.min.js

'use strict';

const Site = {
    init: () => {
        Site.Base.init();
    },
    Base: {
        init: () => {
            
        }
    }
}

$(document).ready(() => Site.init());